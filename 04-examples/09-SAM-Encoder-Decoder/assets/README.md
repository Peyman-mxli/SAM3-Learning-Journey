# Assets

This example requires no input images because it is an architectural timing simulation.

The `output/` directory contains the caching comparison chart generated from the validated Colab exercise.
