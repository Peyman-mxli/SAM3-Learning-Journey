# Assets — Advanced MaskAnnotator and SAM2

This directory contains the visual assets generated while testing the examples from:

**07 — Advanced MaskAnnotator and SAM2**

The assets provide visual evidence of the YOLOv8 + SAM 3 segmentation workflows implemented and validated in Google Colab.

---

## Directory Structure

```text
assets/
├── README.md
└── output/
    ├── 01_basic_mask_annotator_output.png
    ├── 02_mask_opacity_output.png
    ├── 03_mask_and_box_annotator_output.png
    ├── 04_person_only_segmentation_output.png
    ├── 05_reusable_bus_output.png
    └── 05_reusable_zidane_output.png
```

---

## Output Assets

The [`output/`](./output/) directory contains the generated results from the validated Session 07 examples.

### 01 — Basic MaskAnnotator

```text
01_basic_mask_annotator_output.png
```

Generated by:

[`../01_basic_mask_annotator.py`](../01_basic_mask_annotator.py)

Validated result:

```text
YOLO detections: 6
SAM masks generated: 6
```

This image demonstrates the basic workflow:

```text
Input Image
     ↓
YOLOv8
     ↓
Bounding Boxes
     ↓
SAM 3
     ↓
Segmentation Masks
     ↓
MaskAnnotator
```

---

### 02 — Mask Opacity

```text
02_mask_opacity_output.png
```

Generated by:

[`../02_mask_opacity.py`](../02_mask_opacity.py)

The example compares:

```text
Opacity 0.2
Opacity 0.5
Opacity 0.9
```

The result demonstrates how mask opacity changes the balance between the original image and the segmentation overlay.

---

### 03 — Mask + Box Annotator

```text
03_mask_and_box_annotator_output.png
```

Generated by:

[`../03_mask_and_box_annotator.py`](../03_mask_and_box_annotator.py)

Validated result:

```text
YOLO detections: 6
SAM masks generated: 6
```

The visualization combines:

```text
Original Image
      +
SAM 3 Masks
      +
YOLO Bounding Boxes
```

This makes it possible to compare rectangular object localization with pixel-level segmentation.

---

### 04 — Person-Only Segmentation

```text
04_person_only_segmentation_output.png
```

Generated by:

[`../04_person_only_segmentation.py`](../04_person_only_segmentation.py)

Validated filtering result:

```text
Total YOLO detections:       6
Person detections:           4
Objects removed before SAM:  2
Person masks generated:      4
```

This output demonstrates the important workflow:

```text
YOLO Detection
      ↓
Class Filtering
      ↓
Person Detections Only
      ↓
SAM 3
      ↓
Person Masks
```

Filtering occurs **before SAM segmentation**.

---

### 05 — Reusable Segmentation Pipeline

Two outputs were generated by:

[`../05_reusable_segmentation_function.py`](../05_reusable_segmentation_function.py)

#### Bus Image

```text
05_reusable_bus_output.png
```

Validated result:

```text
YOLO detections: 6
SAM masks:       6
```

#### Zidane Image

```text
05_reusable_zidane_output.png
```

Validated result:

```text
YOLO detections: 3
SAM masks:       3
```

These outputs demonstrate that the same reusable function can process different images without rewriting the segmentation pipeline.

```text
bus.jpg
    ↓
segment_image()
    ↓
Result

zidane.jpg
    ↓
segment_image()
    ↓
Result
```

---

## Example 06

`06_sam2_temporal_concept.py` does not generate an image.

The example is intentionally conceptual and demonstrates:

- Static segmentation
- Temporal segmentation
- Temporal memory
- Mask propagation
- The conceptual transition toward SAM2 video segmentation

Therefore, there is no corresponding output image for Example 06.

---

## Validation

All six Python examples were executed successfully in Google Colab.

```text
01_basic_mask_annotator.py              ✅ PASSED
02_mask_opacity.py                      ✅ PASSED
03_mask_and_box_annotator.py            ✅ PASSED
04_person_only_segmentation.py          ✅ PASSED
05_reusable_segmentation_function.py    ✅ PASSED
06_sam2_temporal_concept.py             ✅ PASSED
```

The first five examples generated six visual outputs.

```text
Generated image outputs: 6
Validated Python examples: 6
```

---

## Purpose

These assets are stored as evidence of the practical results produced by the example scripts.

They demonstrate the progression from:

```text
Object Detection
       ↓
SAM 3 Segmentation
       ↓
Mask Visualization
       ↓
Opacity Customization
       ↓
Bounding Box + Mask Composition
       ↓
Selective Segmentation
       ↓
Reusable Segmentation Pipelines
```

---

## Status

```text
Output images generated: 6
Examples validated:       6

Status: COMPLETE ✅
```
