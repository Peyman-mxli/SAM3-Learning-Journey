# Practical Assets

This directory documents the inputs and outputs for the Lesson 09 encoder-decoder caching experiment.

```text
assets/
├── README.md
└── output/
    ├── README.md
    └── sam_encoder_decoder_caching_comparison.png
```

The practical is a computational simulation, so it does not require a real image file or SAM checkpoint.
