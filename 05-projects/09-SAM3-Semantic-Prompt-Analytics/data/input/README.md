# Input Data

`bus.jpg` is the validated Session 08 image used for semantic text-prompt segmentation.

It supports multiple concepts in the same scene:

```text
vehicle
bus
person
wheel
```

The pipeline reads this file without modifying it.
