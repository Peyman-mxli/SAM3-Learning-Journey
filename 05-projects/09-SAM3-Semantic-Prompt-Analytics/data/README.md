# Data

This directory separates immutable project inputs from generated visual evidence.

```text
data/
├── README.md
├── input/
└── output/
```

Original images are read from `input/`. Generated visualizations are written to `output/`.
