# Results

This directory preserves structured evidence from the validated Session 08 execution used to design Project 09.

```text
results/
├── README.md
├── json/
└── csv/
```

JSON provides a hierarchical report. CSV provides prompt-level and detection-level tables.

Only measured values are included. Detailed object metrics are limited to the `person` prompt because those were the values printed and verified during execution.
