# Results

This directory contains structured evidence generated by the validated standalone Project 09 execution.

```text
results/
├── README.md
├── json/
└── csv/
```

JSON provides a hierarchical report. CSV provides prompt-level and detection-level tables.

The final JSON contains 10 object records across four prompts. The detection CSV contains 10 rows, and the prompt-summary CSV contains four rows.
