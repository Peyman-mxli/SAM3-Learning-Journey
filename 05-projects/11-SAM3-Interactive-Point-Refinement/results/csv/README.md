# CSV Results

Expected files:

- `object_point_metrics.csv` — point, class, mask area, and confidence for each object.
- `refinement_summary.csv` — positive/negative points and before/after refinement metrics.
