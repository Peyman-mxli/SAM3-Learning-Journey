{
  "type": "function",
  "function": {
    "name": "segment_with_sam3",
    "description": "Segment objects matching a semantic prompt and return measured masks.",
    "parameters": {
      "type": "object",
      "properties": {
        "media_path": {"type": "string"},
        "prompt": {"type": "string"},
        "confidence": {"type": "number", "minimum": 0, "maximum": 1}
      },
      "required": ["media_path", "prompt"],
      "additionalProperties": false
    }
  }
}
