# Security

Never commit Hugging Face tokens, API keys, checkpoint download URLs containing credentials, or private input media. Keep secrets in environment variables. Review third-party model code before enabling remote code execution. Treat model-generated tool arguments as untrusted and validate paths, types, ranges, and output size.
