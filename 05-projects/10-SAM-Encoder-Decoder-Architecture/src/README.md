# Source

`encoder_decoder_benchmark.py` is the project entry point.

It validates configuration, runs cached and uncached trials, calculates aggregate statistics, and exports CSV, JSON, and chart artifacts.

```bash
python src/encoder_decoder_benchmark.py
```
