# Configuration

`benchmark.json` defines the prompt counts, number of trials, and simulated component delays.

The default delays are intentionally short so the full validation suite runs quickly. Increase them to reproduce the longer classroom demonstration.

All prompt counts and trial values must be positive. Delay values must be non-negative.
